version: "3.8"

services:
  filestore-backend:
    build: ./backend
    container_name: filestore-backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    restart: unless-stopped
